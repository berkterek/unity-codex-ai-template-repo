# Update Claude MD — Legacy Alias

`/update-claude-md` is a deprecated compatibility alias for
`.codex/packs/unity-game/commands/update-agents-md.md`.

Use `/update-agents-md` for new Codex workflows.

## Usage

```
/update-claude-md
/update-claude-md --section rules
/update-claude-md --section commands
/update-claude-md --section agents
```

## Workflow

1. Read `.codex/packs/unity-game/commands/update-agents-md.md`.
2. Execute it with the same arguments.
3. Report that the legacy command name was used and recommend
   `/update-agents-md` for future runs.
